class Solution {
    public double angleClock(int hour, int minutes) {
        if(hour==12)hour=0;
        hour=hour*60+minutes;
        double hourAngle=hour*0.5,minuteAngle=minutes*6.0;
        double total=Math.abs(minuteAngle-hourAngle);
        return Math.min(total,360-total);
    }
}