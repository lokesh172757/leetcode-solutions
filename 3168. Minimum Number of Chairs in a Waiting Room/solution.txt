class Solution {
    public int minimumChairs(String s) {
        int max=0,curr=0;
        for(int i=0;i<s.length();i++){
            if(s.charAt(i)=='E'){
                curr++;
                max=Math.max(max,curr);
            }
            else curr--;
        }
        return max;
    }
}