class Solution {
    public int bitwiseComplement(int n) {
        if(n==0)return 1;
        int ans=0,bit=0;
        while(n>0){
            int dig=n%2;
            if(dig==1)ans+=(0<<bit);
            else ans+=(1<<bit);
            n/=2;
            bit++;
        }
        return ans;
    }
}