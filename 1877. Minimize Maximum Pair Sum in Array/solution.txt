class Solution {
    public int minPairSum(int[] nums) {
        Arrays.sort(nums);
        int n=nums.length;
        int ans=nums[0]+nums[n-1];
        for(int i=1;i<n/2;i++){
            ans=Math.max(nums[i]+nums[n-i-1],ans);
        }
        return ans;
    }
}