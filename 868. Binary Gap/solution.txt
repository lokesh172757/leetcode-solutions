class Solution {
    public int binaryGap(int n) {
        int ans=0,pos=1,last=-1;
        while(n>0){
            if(n%2==1){
                if(last!=-1)ans=Math.max(ans,pos-last);
                last=pos;
            }
            n/=2;
            pos++;
        }
        return ans;
    }
}