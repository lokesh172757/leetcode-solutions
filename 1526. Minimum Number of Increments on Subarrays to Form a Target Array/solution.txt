class Solution {
    public int minNumberOperations(int[] nums) {
        int ans=0;
        int prev=0;
        for(int i=0;i<nums.length;i++){
            if(nums[i]>prev){
                ans+=nums[i]-prev;
                
            }
            prev=nums[i];
        }
        return ans;
    }
}