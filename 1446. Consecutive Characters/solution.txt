class Solution {
    public int maxPower(String s) {
        int i=0,j=0,ans=0;
        while(j<s.length()){
            while(i<j&&(s.charAt(i)!=s.charAt(j)))i++;
            ans=Math.max(ans,j-i+1);
            j++;
        }
        return ans;
    }
}